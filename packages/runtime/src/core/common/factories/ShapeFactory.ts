import { RuntimeStore, RuntimeSystem } from "runtime-framework"

import { ShapeModel } from "../../../model/world/ShapeModel"
import { Meta } from "../../Meta"

export const newShape = (meta: Meta, store: RuntimeStore<RuntimeSystem>, shape: ShapeModel) => {
    void 0   
}