import { RuntimeSystemStack } from "runtime/src/core/RuntimeSystemStack"

import { EntityStore } from "../../../../runtime-framework/src"

export function RuntimeView(props: { store: EntityStore, stack: RuntimeSystemStack }) {
    return (
        <>

        </>
    )
}