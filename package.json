{
    "private": true,
    "scripts": {
        "dev": "turbo run dev --no-daemon",
        "test": "turbo run test --no-daemon",
        "build": "turbo run build --no-daemon"
    },
    "workspaces": [
        "packages/*",
        "packages-dev/*"
    ],
    "devDependencies": {
        "@turbo/gen": "^1.9.8",
        "config": "*",
        "eslint": "^8.41.0",
        "turbo": "^1.10.1"
    }
}
